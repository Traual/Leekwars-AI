include("z*");
global leek2 = nearestrealenemy();
var enemy = getNearestEnemy();
var PotoRandom = getNearestAlly();
var dist = getCellDistance(getCell(), getCell(leek2));
var disto = getPathLength(getCell(), getCell(leek2));

if (getScience(leek2)>=200 && getStrength(leek2)>=600 && getCooldown(CHIP_TELEPORTATION)==0 ) {

while (dist>18 && getMP()>1) {
moveToward(leek2, 1);	
}

useChipOnCell(CHIP_TELEPORTATION, getCellToUseChip(CHIP_LIBERATION, leek2));
useChip(CHIP_LIBERATION, leek2);
}

if (getWeapon()==null) {
	setWeapon(WEAPON_FLAME_THROWER);
}

if (getRelativeShield(leek2)>=10) {
if (getWeapon()!=WEAPON_GAZOR) {
	setWeapon(WEAPON_GAZOR);
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}	
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
}

if (getAbsoluteShield(leek2)>=10) {
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}	
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}if (getTP()>=14) {
moveTowardCell(getCellToUseWeapon(WEAPON_GAZOR, leek2,1));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
}
}

if (getLife()<=400) {
useChip(CHIP_REGENERATION, getLeek());
}

if (getPathLength(getCell(), getCellToUseWeapon(WEAPON_FLAME_THROWER, leek2))>getMP() && getTP()>=getTotalTP()) {
useChipOnCell(CHIP_TELEPORTATION, getCellToUseWeapon(WEAPON_FLAME_THROWER, leek2));	
useWeapon(leek2);
useWeapon(leek2);
useWeapon(leek2);
}

if (getPathLength(getCell(), getCellToUseWeapon(WEAPON_FLAME_THROWER, leek2))<=getMP() && getTP()>=12) {
if (getWeapon()!=WEAPON_FLAME_THROWER) {
setWeapon(WEAPON_FLAME_THROWER);	
}
moveTowardCell(getCellsToUseWeapon(WEAPON_FLAME_THROWER, leek2));


useWeapon(leek2);


useWeapon(leek2);


useWeapon(leek2);

}

if (getPathLength(getCell(), getCellToUseWeapon(WEAPON_KATANA, leek2))<=getMP() && getTP()>=14) {
if (getWeapon()!=WEAPON_KATANA) {
setWeapon(WEAPON_KATANA);	
}
moveTowardCell(getCellsToUseWeapon(WEAPON_KATANA, leek2));


useWeapon(leek2);


useWeapon(leek2);


useWeapon(leek2);

}

if (getPathLength(getCell(), getCellToUseWeapon(WEAPON_ELECTRISOR, leek2))<=getMP() && getTP()>=14 && getLevel()>=257) {
if (getWeapon()!=WEAPON_ELECTRISOR) {
setWeapon(WEAPON_ELECTRISOR);	
}
moveTowardCell(getCellsToUseWeapon(WEAPON_ELECTRISOR, leek2));

if (isAlive(leek2)) {
useWeapon(leek2);
}
if (isAlive(leek2)) {
useWeapon(leek2);
}
if (isAlive(leek2)) {
useWeapon(leek2);
}
}

if (getPathLength(getCell(), getCellToUseWeapon(WEAPON_DESTROYER, leek2))<=getMP() && getTP()>=18) {
if (getWeapon()!=WEAPON_DESTROYER) {
setWeapon(WEAPON_DESTROYER);	
}
moveTowardCell(getCellsToUseWeapon(WEAPON_DESTROYER, leek2));

if (isAlive(leek2)) {
useWeapon(leek2);
}
if (isAlive(leek2)) {
useWeapon(leek2);
}
if (isAlive(leek2)) {
useWeapon(leek2);
}
}

if (getStrength(leek2)>=500 and getCooldown(CHIP_LIBERATION) == 0 && getScience(leek2)>=200) {
	moveTowardCell(getCellToUseWeapon(leek2));
		useChip(CHIP_LIBERATION, leek2);
	}


if (bouclier() == true and getCooldown(CHIP_LIBERATION) == 0) {
	 moveTowardCell(getCellToUseWeapon(leek2));
		useChip(CHIP_LIBERATION, leek2);
	}

if (canUseChip(CHIP_METEORITE, leek2)) {
	useChip(CHIP_METEORITE, leek2);
}

if (canUseChip(CHIP_TOXIN, leek2)) {
	useChip(CHIP_TOXIN, leek2);
}

if (canUseChip(CHIP_FRACTURE, leek2)) {
	useChip(CHIP_FRACTURE, leek2);
}

summon(CHIP_METALLIC_BULB, getCellToUseChip(CHIP_METALLIC_BULB, getLeek()), bulbeMetalique);
summon(CHIP_HEALER_BULB, getCellToUseChip(CHIP_HEALER_BULB, getLeek()), bulbeSoigneur);

if (getTP()<getTotalTP()) {
	moveTowardCell(tableaucellscacher());
}

debug(getOperations());


































































































































