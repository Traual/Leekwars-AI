include("Cache_cache.leek");
global leek2=nearestrealenemy();
global me = getLeek();
var Traual = getName(55595);
global poto = nearestrealally();
global leek4 = realally();

moveToward(getFarthestAlly());	


if (getStrength(getNearestAlly())<=1200 && getStrength(getNearestAlly())>=200) {
useChip(CHIP_PROTEIN, getNearestAlly());
useChip(CHIP_STEROID, getNearestAlly());
useChip(CHIP_DOPING, getNearestAlly());
}
if (getAgility()<=400) {
useChip(CHIP_REFLEXES, getLeek());
useChip(CHIP_STRETCHING, getLeek());
}

if (getAgility()>400) {
useChip(CHIP_THORN, getNearestAlly());	
}

if (getMP()<=5) {
useChip(CHIP_LEATHER_BOOTS, getLeek());
}
if (getTP()<=12) {
useChip(CHIP_MOTIVATION, getLeek());	
}


useChip(CHIP_ADRENALINE, getNearestAlly());
useChip(CHIP_WINGED_BOOTS, getNearestAlly());

if (getTP()<=3 && getPathLength(getCell(), getCell(getNearestAlly()))<=6) {
moveTowardCell(tableaucellscacher());	
}















































































































