include("Cache_cache.leek");
global leek2 = nearestrealenemy();
if (getLife(leek2) == 0) {leek2 = nearestrealenemy();
	
}
var enemy = getNearestEnemy();
var mycell = getCell();
var enemycell = getCell(enemy);
var dist = getDistance(mycell, getCell(leek2));
var tour = getTurn();
var disto = getPathLength(mycell, getCell(leek2));


// Ia 
if (getTurn()==1) {
setWeapon(WEAPON_GAZOR);	
}

if (dist > 12 and tour == 2) {
	summon(CHIP_ROCKY_BULB, getCellToUseChip(CHIP_ROCKY_BULB, getLeek()), bulbeRocheux);

}
if (getLife() < getTotalLife() - 400 and getCooldown(CHIP_REGENERATION) == 0) {
	useChip(CHIP_REGENERATION, getLeek());
	useChip(CHIP_VACCINE, getLeek());
	if (getCooldown(CHIP_REGENERATION) > 0) {
		moveTowardCell(tableaucellscacher());
		
	}


}
poisoncounter();
	moveTowardCell(getCellToUseWeapon(WEAPON_ELECTRISOR, leek2));
	if (isOnSameLine(getCell(), getCell(leek2)) && canUseWeapon(WEAPON_GAZOR, leek2) && dist >=3) {
	useWeapon(leek2);
	useWeapon(leek2);
	}
	if (dist >=4) {
	useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
	useWeaponOnCell(useCHiporWeaponarea(WEAPON_GAZOR, 3, leek2));
	}
	
	
	
	
	
	if (getTP() < 6) {
		moveTowardCell(tableaucellscacher());
    }
	


summon(CHIP_ICED_BULB, getCellToUseChip(CHIP_ICED_BULB, getLeek()), bulbAI);
summon(CHIP_ROCKY_BULB, getCellToUseChip(CHIP_ROCKY_BULB, getLeek()), bulbeRocheux);
summon(CHIP_PUNY_BULB, getCellToUseChip(CHIP_PUNY_BULB, getLeek()), bulbechetif);

if (getLife()<getTotalLife()) { useChip(CHIP_VACCINE, getLeek());	
}

if (dist<=4 && canUseWeapon(WEAPON_FLAME_THROWER, leek2)) {
setWeapon(WEAPON_FLAME_THROWER);
useWeapon(leek2);
useWeapon(leek2);
setWeapon(WEAPON_GAZOR);
}






if (canUseChip(CHIP_VENOM, leek2)) {
useChip(CHIP_VENOM, leek2);	
}

if (canUseChip(CHIP_TRANQUILIZER, leek2)) {
useChip(CHIP_TRANQUILIZER, leek2);	
}
useChip(CHIP_ARMOR, getLeek());