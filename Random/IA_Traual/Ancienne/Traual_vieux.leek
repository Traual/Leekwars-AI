
var enemy=getNearestEnemy();
var me = getLeek();
var caseE = getCell(enemy);
var case = getCell();
var restants = getAliveEnemies();



if (getLife(me)<400) {useChip(CHIP_REGENERATION, me);}


 if (canUseChip(CHIP_STALACTITE, enemy)) {useChip(CHIP_STALACTITE, enemy);}

if (canUseChip(CHIP_LIGHTNING, enemy))
{useChip(CHIP_LIGHTNING, enemy);
useChip(CHIP_LIGHTNING, enemy);
useChip(CHIP_LIGHTNING, enemy);}

if (canUseChip(CHIP_METEORITE, enemy)) {useChip(CHIP_METEORITE, enemy);}

if (getWeapon()!=WEAPON_GRENADE_LAUNCHER && canUseWeapon(WEAPON_AXE, enemy)==false) {setWeapon(WEAPON_GRENADE_LAUNCHER);}
if (getWeapon()!=WEAPON_AXE && canUseWeapon(WEAPON_AXE, enemy)){setWeapon(WEAPON_AXE);} 
if (canUseWeapon(enemy)) {useWeapon(enemy);
useWeapon(enemy);}

if (canUseChip(CHIP_LIBERATION, enemy)) {useChip(CHIP_LIBERATION, enemy);}

function bulbAI2() 
    {var summoner = getSummoner();
    moveToward(summoner);
	if (getLife(summoner)<500) {useChip(CHIP_BANDAGE, summoner);}
    useChip(CHIP_PROTEIN, summoner);
	useChip(CHIP_HELMET, summoner);
	useChip(CHIP_PEBBLE, getNearestEnemy());}

if (getLife(enemy)>100) {summon(CHIP_PUNY_BULB, getCellFromXY(getCellX(getCell())-1, getCellY(getCell())-1), bulbAI2);}


if (canUseChip(CHIP_ARMOR, me)) {useChip(CHIP_SHIELD, me);
useChip(CHIP_ARMOR, me);}

if (getLife()<500) {useChip(CHIP_CURE, me);}




