
include("Fonctions_Utiles");
if (getLife(leek2) == 0) {leek2 = nearestrealenemy();	
}
var enemy = getNearestEnemy();
var mycell = getCell();
var enemycell = getCell(enemy);
var dist = getDistance(mycell, getCell(leek2));
var tour = getTurn();
var disto = getPathLength(mycell, getCell(leek2));
var leek2cell = getCell(leek2);


// Ia 
if (getLife() < getTotalLife() - 800 and getCooldown(CHIP_REGENERATION) == 0) {
	useChip(CHIP_REGENERATION, getLeek());
	useChip(CHIP_VACCINE, getLeek());

}
poisoncounter();

//B

if (getStrength(leek2)>=500 and getCooldown(CHIP_LIBERATION) == 0 && getScience(leek2)>=200) {
	if (getCooldown(CHIP_ICEBERG) == 0 && getCellToUseChip(CHIP_LIBERATION, leek2)<=getMP()) {
		moveTowardCell(getCellToUseChip(CHIP_ICEBERG, leek2));
		useChip(CHIP_LIBERATION, leek2);

		if (getCooldown(CHIP_LIBERATION) > 0) {
			useChipOnCell(CHIP_ICEBERG, useCHiporWeaponarea(CHIP_ICEBERG, 2, leek2));
			useChip(CHIP_STALACTITE, leek2);
		}
	}
	if (getCooldown(CHIP_ICEBERG) > 0 && getCooldown(CHIP_STALACTITE) == 0) {
		moveTowardCell(getCellToUseChip(CHIP_LIBERATION, leek2));
		useChip(CHIP_LIBERATION, leek2);
		if (getCooldown(CHIP_LIBERATION) > 0) {
			useChip(CHIP_STALACTITE, leek2);
		}
	}
	if (getCooldown(CHIP_ICEBERG) > 0 && getCooldown(CHIP_STALACTITE) > 0) {
		moveTowardCell(getCellToUseWeapon(WEAPON_LASER, leek2));
		useChip(CHIP_LIBERATION, leek2);
		if (getWeapon() != WEAPON_LASER) {
			setWeapon(WEAPON_LASER);

		}
		if (getCooldown(CHIP_LIBERATION) > 0) {
			useWeapon(leek2);
		}
	}
}


if (bouclier() == true and getCooldown(CHIP_LIBERATION) == 0 && getCellToUseChip(CHIP_LIBERATION, leek2)<=getMP()) {
	if (getCooldown(CHIP_ICEBERG) == 0) {
		moveTowardCell(getCellToUseChip(CHIP_ICEBERG, leek2));
		useChip(CHIP_LIBERATION, leek2);

		if (getCooldown(CHIP_LIBERATION) > 0) {
			useChipOnCell(CHIP_ICEBERG, useCHiporWeaponarea(CHIP_ICEBERG, 2, leek2));
		}
	}
	if (getCooldown(CHIP_ICEBERG) > 0 && getCooldown(CHIP_STALACTITE) == 0 && getCooldown(CHIP_LIBERATION) == null) {
		moveTowardCell(getCellToUseChip(CHIP_LIBERATION, leek2));
		useChip(CHIP_LIBERATION, leek2);
		if (getCooldown(CHIP_LIBERATION) > 0) {
			useChip(CHIP_STALACTITE, leek2);
		}
	}
}

if (getMP()>disto && getAbsoluteShield()>=10 && getAbsoluteShield(leek2)<=15 && getRelativeShield(leek2)<=15 && getCooldown(CHIP_DEVIL_STRIKE)==null) {
moveToward(leek2);
if (dist<=3) {
useChip(CHIP_DEVIL_STRIKE, getLeek());
}
}

if (getMP()>disto && getRelativeShield()>=10 && getAbsoluteShield(leek2)<=15 && getRelativeShield(leek2)<=15 && getCooldown(CHIP_DEVIL_STRIKE)==null) {
moveToward(leek2);
if (dist<=3) {
useChip(CHIP_DEVIL_STRIKE, getLeek());
}
}

if (getPathLength(getCell(), getCellToUseWeapon(WEAPON_M_LASER, leek2))<=getMP() && getStrength()>=350 && getTP()>=16) {
if (getWeapon()!=WEAPON_M_LASER) {
setWeapon(WEAPON_M_LASER);
useChip(CHIP_DOPING, getLeek());
}

moveTowardCell(getCellToUseWeapon(WEAPON_M_LASER, leek2));

if (isAlive(leek2)) {
useWeapon(leek2);	
}
if (isAlive(leek2)) {
useWeapon(leek2);	
}
if (isAlive(leek2)) {
useWeapon(leek2);	
}
if (isAlive(leek2)) {
useWeapon(leek2);	
}
}

if (getAbsoluteShield(leek2) <= 1 && getRelativeShield(leek2) <= 1 && getPathLength(getCell(), getCellToUseChip(CHIP_LIGHTNING, leek2))<=getMP() && getTP()>=8) {
	moveTowardCell(getCellToUseChip(CHIP_LIGHTNING, leek2));
	
	if (getLife(leek2) > 0) {
		useChipOnCell(CHIP_LIGHTNING, leek2cell);
	}
	if (getLife(leek2) > 0) {
		useChipOnCell(CHIP_LIGHTNING, leek2cell);
		}
	if (getLife(leek2) > 0) {
		useChipOnCell(CHIP_LIGHTNING, leek2cell);
		}
	if (getLife(leek2) > 0) {
		useChipOnCell(CHIP_LIGHTNING, leek2cell);
		}
		if (getLife(leek2) > 0) {
		useChipOnCell(CHIP_LIGHTNING, leek2cell);
		}
		if (getLife(leek2) > 0) {
		useChipOnCell(CHIP_LIGHTNING, leek2cell);
		}
	if (getTP() < 2) {
		moveTowardCell(tableaucellscacher());

	}
}

if (canUseWeapon(WEAPON_KATANA, leek2)) {


	if (getWeapon() != WEAPON_KATANA) {
		setWeapon(WEAPON_KATANA);

	}
	if (isAlive(leek2)) {
		useWeapon(leek2);
	}
	if (isAlive(leek2)) {
		useWeapon(leek2);
	}
	if (isAlive(leek2)) {
		useWeapon(leek2);
	}
	if (isAlive(leek2)) {
		useWeapon(leek2);
	}
	if (getWeapon()!=WEAPON_M_LASER) {
setWeapon(WEAPON_M_LASER);	
}
}

if (getMP()>getPathLength(getCellToUseWeapon(WEAPON_ELECTRISOR, leek2), mycell) &&
getTP()>=14) {
if (getWeapon()!=WEAPON_ELECTRISOR) {
setWeapon(WEAPON_ELECTRISOR);	
}	
moveTowardCell(getCellToUseWeapon(WEAPON_ELECTRISOR, leek2));

if (isAlive(leek2)) {
		useWeapon(leek2);
	}
	if (isAlive(leek2)) {
		useWeapon(leek2);
	}
	if (isAlive(leek2)) {
		useWeapon(leek2);
	}
	if (isAlive(leek2)) {
		useWeapon(leek2);
	}
	if (getWeapon()!=WEAPON_M_LASER) {
setWeapon(WEAPON_M_LASER);	
}
}

if (getStrength()>=500 && getMP()>getPathLength(getCell(), getCellToUseChip(CHIP_METEORITE, leek2))) {
moveTowardCell(getCellToUseChip(CHIP_METEORITE, leek2));
if (isAlive(leek2)) {
useChipOnCell(CHIP_METEORITE,leek2cell);	
}
}

if (getStrength()<=750) {
useChip(CHIP_DOPING, getLeek());
useChip(CHIP_PROTEIN, getLeek());
}

if (getStrength()>=500 && getMP()>getPathLength(getCell(), getCellToUseChip(CHIP_METEORITE, leek2))) {
moveTowardCell(getCellToUseChip(CHIP_METEORITE, leek2));
if (isAlive(leek2)) {
useChipOnCell(CHIP_METEORITE, useCHiporWeaponarea(CHIP_METEORITE, 2, leek2));
}
}

if (getCooldown(CHIP_LIBERATION) > 0 && dist <= 11 && disto > 5 && bouclier() == true && getPathLength(getCell(), getCellToUseWeapon(WEAPON_GRENADE_LAUNCHER, leek2))<=getMP()) {
	moveTowardCell(getCellToUseWeaponOnCell(WEAPON_GRENADE_LAUNCHER, leek2));

	if (getWeapon() != WEAPON_GRENADE_LAUNCHER) {
		setWeapon(WEAPON_GRENADE_LAUNCHER);

	}
	if (getLife(leek2)>0) {
		useWeaponOnCell(useCHiporWeaponarea(WEAPON_GRENADE_LAUNCHER, 2, leek2));
	}
	if (getLife(leek2)>0) {
		useWeaponOnCell(useCHiporWeaponarea(WEAPON_GRENADE_LAUNCHER, 2, leek2));
	}
	
	
}
summon(CHIP_METALLIC_BULB, getCellToUseChip(CHIP_METALLIC_BULB, getLeek()), bulbeMetalique);
summon(CHIP_HEALER_BULB, getCellToUseChip(CHIP_HEALER_BULB, getLeek()), bulbeSoigneur);

if (getLife()<getTotalLife()) { useChip(CHIP_VACCINE, getLeek());}

if (isAlive(enemy)) {
	useChipOnCell(CHIP_LIGHTNING, useCHiporWeaponarea(CHIP_LIGHTNING, 2, leek2));
}
if (isAlive(enemy)) {
	useChipOnCell(CHIP_LIGHTNING, useCHiporWeaponarea(CHIP_LIGHTNING, 2, leek2));
}
if (isAlive(enemy)) {
	useChipOnCell(CHIP_LIGHTNING, useCHiporWeaponarea(CHIP_LIGHTNING, 2, leek2));
}
if (isAlive(enemy)) {
	useChipOnCell(CHIP_LIGHTNING, useCHiporWeaponarea(CHIP_LIGHTNING, 2, leek2));
}
	


say(lama());
	

if (getTP()<=16) {moveTowardCell(tableaucellscacher());}

if (getStrength()>getStrength(leek2) && disto>=20) {
moveToward(leek2);	
}
else
moveAwayFrom(leek2);


debug(getOperations());



















