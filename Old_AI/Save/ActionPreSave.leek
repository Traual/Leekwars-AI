/**
 * The Action Class : To Then Manipulate the object better
**/

include("../includes.leek")

class ActionPreSave {
	TO
	FROM
	MP_SCORE
	constructor(cellTo, cellFrom, mpScore) {
		this.TO = cellTo
		this.FROM = cellFrom
		this.MP_SCORE = mpScore
	}
}