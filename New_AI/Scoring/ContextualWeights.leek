/*
 * Poids contextuels. La famille de scoring v3 n'emploie aucun réseau : le commutateur reste,
 * fermé, pour que la boucle de jeu garde un seul point de choix des poids de placement.
 */

include("../includes.leek")

global CONTEXTUAL_WEIGHTS_ENABLED = false

class ContextualWeightNetworkClass {
	static build(State, mySide) {
		return HistoricalWeightsFactory.build(State, mySide)
	}
}
